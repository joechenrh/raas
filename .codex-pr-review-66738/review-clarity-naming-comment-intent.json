{
  "findings": [
    {
      "severity": "Major",
      "title": "Prefer a named read-range result instead of the new `[2][]uint64` tuple",
      "why": "The renamed helper no longer communicates what each slot means. Callers now have to know that `[0]` is the per-file start offsets and `[1]` is the per-file property end offsets, but the doc comment still says the function returns only an offsets list per file for each key.",
      "scope": "pkg/lightning/backend/external/util.go:56-69, pkg/lightning/backend/external/engine.go:530-531, pkg/lightning/backend/external/merge_v2.go:127-142",
      "anchor": {
        "path": "pkg/lightning/backend/external/util.go",
        "line": 69,
        "side": "RIGHT"
      },
      "risk_if_unchanged": "Future callers can easily swap the tuple indexes or read `readRangesPerKey` as a complete range object, which makes this helper harder to reuse safely outside the current call sites.",
      "evidence": "`getReadRangeFromProps` now returns `[][2][]uint64`, while the changed call sites immediately consume magic indexes (`readRangesPerKey[start][0]`, `readRangesPerKey[end-1][1]`, `readRanges[0][0]`, `readRanges[1][1]`). The updated comment at `util.go` still describes only the old single-offset behavior, so the tuple layout is not documented where the API is declared.",
      "change_request": "Prefer a named result type or struct with fields like `startOffsets` and `endOffsets`, and update the helper comment to describe the two returned positions explicitly instead of relying on `[0]` and `[1]`."
    },
    {
      "severity": "Minor",
      "title": "`readAllData`'s new `endOffsets` parameter name overstates what the function actually does",
      "why": "The new signature reads as if both offset arrays bound the actual file reads, but only `startOffsets` is passed to `readOneFile`. `endOffsets` is used only to estimate size and pick concurrency.",
      "scope": "pkg/lightning/backend/external/reader.go:37-45, pkg/lightning/backend/external/reader.go:74-97, pkg/lightning/backend/external/reader.go:111-126",
      "anchor": {
        "path": "pkg/lightning/backend/external/reader.go",
        "line": 42,
        "side": "RIGHT"
      },
      "risk_if_unchanged": "The next maintainer can infer that `readAllData` now enforces both offset bounds and reuse the API under that assumption, even though the stop condition is still `endKey` inside `readOneFile`.",
      "evidence": "After the signature change, `readAllData` computes `size := endOffsets[i] - startOffsets[i]` and logs `endOffset`, but the downstream call at `reader.go:111-126` still forwards only `startOffsets[fileIdx]` into `readOneFile`. No name or comment explains that `endOffsets` is only a sizing or concurrency hint.",
      "change_request": "Please rename this parameter to something that matches its role, such as `rangeEstimateEndOffsets`, or add a short why-comment stating that the values are only used for concurrency estimation and logging, not as hard read bounds."
    }
  ]
}
