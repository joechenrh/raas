Invoke skill 
a directly.

Inputs: , , .
Write output JSON to exactly .
Run this subagent with write-capable filesystem access matching the parent execution mode.
Do not run in parent; execute in this subagent only.
Do not delegate to additional subagents.
Reviewer child processes must not call spawn_agent, collab, Task, or delegate to any additional subagents.
Keep the working directory at /mnt/data/joechenrh/raas and use the reviewed checkout only via the provided writable scope.
