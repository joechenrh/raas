I could not complete this subagent review because file access in the subagent is blocked before any command starts.

Concrete failure: every `exec_command` call, including a minimal `pwd` in `/mnt/data/joechenrh/raas`, fails with:

```text
error applying legacy Linux sandbox restrictions: Sandbox(LandlockRestrict)
```

That prevents me from opening the registered skill files, reading `/mnt/data/joechenrh/raas/repos/pingcap/tidb/worktrees/pr-66738/66738.diff`, or writing `review-clarity-naming-comment-intent.json` with an evidence-based review. I did not create the output file.