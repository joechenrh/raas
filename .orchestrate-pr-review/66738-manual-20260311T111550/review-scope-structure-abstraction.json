{
  "findings": [
    {
      "severity": "Minor",
      "title": "Read-range planning exposes a positional tuple contract to every caller",
      "why": "The new prop-range helper returns `[][2][]uint64`, and the reader path now takes separate `startOffsets` and `endOffsets` slices. That pushes a positional `[start/end][per-file]` convention into every caller instead of expressing the contract with named fields.",
      "scope": "pkg/lightning/backend/external/util.go:64; pkg/lightning/backend/external/reader.go:42; pkg/lightning/backend/external/engine.go:530; pkg/lightning/backend/external/merge_v2.go:141",
      "anchor": {
        "path": "pkg/lightning/backend/external/util.go",
        "line": 64,
        "side": "RIGHT"
      },
      "risk_if_unchanged": "Future call sites can swap tuple indexes or pair mismatched slices without a type error, which makes this hot path harder to review and easier to regress with subtle partial-range read mistakes.",
      "evidence": "`getReadRangeFromProps` now returns `[][2][]uint64`, then `LoadIngestData` and `MergeOverlappingFilesV2` unpack it with expressions like `readRangesPerKey[start][0]`, `readRangesPerKey[end-1][1]`, `readRanges[0][0]`, and `readRanges[1][1]` before handing the slices to `readAllData`. The same positional unpacking pattern was also copied into `bench_test.go`, `reader_test.go`, and `testutil.go`.",
      "change_request": "Prefer a small named result type such as `ReadRange{StartOffsets, EndOffsets}` and pass that through the reader path instead of raw tuple indexes plus sibling slices. That keeps the helper local to one domain contract and removes the magic-index convention from every caller."
    }
  ]
}
