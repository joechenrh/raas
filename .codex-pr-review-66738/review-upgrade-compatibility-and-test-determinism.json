{"findings":[{"severity":"Nit","title":"Fixed sleep makes the new concurrency-limit test timing-dependent","why":"The determinism checklist explicitly calls out sleep-based synchronization. This test already has an explicit signal for the condition it cares about, so the extra 100ms delay makes the assertion depend on wall-clock time instead of only on the synchronization it introduced.","scope":"pkg/lightning/backend/external/util_test.go:189-234 (`TestGetReadRangeFromPropsLimitsParallelRead`)","anchor":{"path":"pkg/lightning/backend/external/util_test.go","line":230,"side":"RIGHT"},"risk_if_unchanged":"The suite pays unnecessary wall-clock cost in CI, and this test becomes an example of timing-based coordination in a path that is otherwise written with explicit synchronization.","evidence":"`blockingOpenMemStorage.Open` updates `s.max` before it sends on `startedCh`, and the test already waits for two `startedCh` events before asserting `store.max.Load() == 2`. The new `time.Sleep(100 * time.Millisecond)` therefore does not validate a new condition; it only inserts timing into the test.","change_request":"Seems no need to sleep here; can we remove the fixed delay and assert immediately after the two `startedCh` events, or use `require.Eventually` if you want to keep the assertion tied to an explicit condition instead of wall-clock waiting?"}]}
